apiVersion: v1
kind: Pod
metadata:
  name: strict-fail-pod
  namespace: dev
spec:
  containers:
  - name: app
    image: alpine
    command: ["sleep", "3600"]
    envFrom:
    - secretRef:
        name: strict-creds
